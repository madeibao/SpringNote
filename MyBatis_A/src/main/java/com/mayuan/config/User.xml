<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test">
    <select id="sql1" parameterType="int" resultType="com.mayuan.myTest.User">
		SELECT id,username as name,sex,address,birthday FROM usera WHERE id = #{id};
	</select>
</mapper>
